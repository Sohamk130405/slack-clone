"use client";

import UserButton from "@/features/auth/components/user-button";

const Home = () => {
  return (
    <div className="h-full">
      <UserButton />
    </div>
  );
};

export default Home;
